/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Nextflow config file for running minimal tests for PacBio Amplified data
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Defines input files and everything required to run a fast and simple pipeline test.

    Use as follows:
        nextflow run sanger-tol/readmapping -profile test_amplified,<docker/singularity> --outdir <OUTDIR>

----------------------------------------------------------------------------------------
*/

process {
    resourceLimits = [
        cpus: 4,
        memory: '15.GB',
        time: '1.h'
    ]
}

params {
    config_profile_name        = 'Test profile for Amplified data'
    config_profile_description = 'Minimal test dataset to check pipeline function on PacBio Amplified data'

    // Input data
    input = "${projectDir}/assets/samplesheet_amplified.csv"

    // Output directory
    outdir = "results_amplified"

    // Alignment
    alignment_format = "bam"
    compression = "none"

    // Fasta references
    fasta = "https://tolit.cog.sanger.ac.uk/test-data/Adalia_bipunctata/reference/insdc/GCA_910592335.1.subset.fa.gz"
}
