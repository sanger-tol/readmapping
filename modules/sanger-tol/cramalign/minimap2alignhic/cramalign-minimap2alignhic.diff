Changes in component 'sanger-tol/cramalign/minimap2alignhic'
'modules/sanger-tol/cramalign/minimap2alignhic/meta.yml' is unchanged
Changes in 'cramalign/minimap2alignhic/main.nf':
--- modules/sanger-tol/cramalign/minimap2alignhic/main.nf
+++ modules/sanger-tol/cramalign/minimap2alignhic/main.nf
@@ -44,11 +44,9 @@
             "-R '${l.replaceAll("\t", "\\\\t")}'"
         }.join(' ')
         : ''
+    def filter_5end = meta.datatype == 'hic' ? 
     """
-    samtools cat ${args1} -r "#:${range[0]}-${range[1]}" ${cram} |\\
-        samtools fastq ${args2} - |\\
-        minimap2 -t${task.cpus} ${args3} ${index} ${rg_arg} - |\\
-        gawk -F'\t' '
+            gawk -F'\t' '
             BEGIN { OFS="\\t" }
             \$1 ~ /^\\@/ { print \$0 }
             \$1 !~ /^\\@/ && and(\$2, 64) > 0 { print 1 \$0 }
@@ -60,6 +58,13 @@
             \$1 ~ /^\\@/ { print \$0 }
             \$1 !~ /^\\@/ { \$2 = and(\$2, compl(2048)); print substr(\$0, 2) }
         ' |\\
+    """ : ''
+    
+    """
+    samtools cat ${args1} -r "#:${range[0]}-${range[1]}" ${cram} |\\
+        samtools fastq ${args2} - |\\
+        minimap2 -t${task.cpus} ${args3} ${index} ${rg_arg} - |\\
+        ${filter_5end} \\
         samtools fixmate ${args4} - - |\\
         samtools view -h ${args5} |\\
         samtools sort ${args6} -@${task.cpus} -T ${prefix}_tmp -o ${prefix}.bam -

'modules/sanger-tol/cramalign/minimap2alignhic/environment.yml' is unchanged
'modules/sanger-tol/cramalign/minimap2alignhic/tests/main.nf.test' is unchanged
'modules/sanger-tol/cramalign/minimap2alignhic/tests/nextflow.config' is unchanged
'modules/sanger-tol/cramalign/minimap2alignhic/tests/main.nf.test.snap' is unchanged
'modules/sanger-tol/cramalign/minimap2alignhic/resources/usr/bin/filter_five_end.pl' is unchanged
************************************************************
